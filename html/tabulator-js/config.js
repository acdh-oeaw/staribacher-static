// holds shared config for all tabulator-js tables

var config = {
    height: 800,
    layout: "fitColumns",
    tooltips: true,
    dataLoader: true,
    responsiveLayout: true,
    maxWidth:"100%",
};
